@model Item

<div class="container">
    <div class="row">
        <div class="col-sm-6">

        </div>
        <div class="options col-sm-6">

            @Html.HiddenFor(c => c.ItemCode)
            @Html.HiddenFor(c => c.Type)
            @Html.HiddenFor(c => c.ParentItemCode)

            @Html.LabelFor(c => c.Quantity)<br />
            @Html.TextBoxFor(c => c.Quantity,
            htmlAttributes: new
            {
                type = "number",
                @class = "form-control form-control-inline input-sm",
                style = "width: 80px"
            })

            <div class="space-20"></div>
            <button type="button" class="btn btn-primary btn-block" data-role="item.addtoorder"><i class="fa-shopping-cart"></i> Add to cart</button>

            @if (Model.AllowOnAutoOrder)
            {
                <div class="text-divider">
                    <h5>or</h5>
                </div>
                            
                <button type="button" class="btn btn-info btn-block" data-role="item.addtoautoorder"><i class="fa-refresh"></i> @Resources.Common.AddToReplenish</button>
            }
        </div>
    </div>
</div>